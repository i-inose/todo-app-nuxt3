import { useAuth } from '@/composables/useTask';

// export default defineNuxtRouteMiddleware(async (to, from) => {
//   // const auth = useAuth();
  
//   // // 認証状態を確認する
//   // await auth.checkAuthState();
  
//   // // 未ログインの場合はログインページにリダイレクト
//   // if (!auth.token.value) {
//   //   return navigateTo('/login');
//   // }
// });
